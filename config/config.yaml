# =============================================================================
# config.yaml — Deprecated compatibility wrapper
# =============================================================================
# Historical note:
#   Older builds loaded a single config.yaml. The platform now separates
#   concerns into:
#     - app.yaml        : platform/runtime defaults & host policy
#     - race_modes.yaml : mode definitions
#     - event.yaml      : event identity & branding
#
# Guidance:
#   Keep this file in the repo for clarity, but do NOT add live settings here.
#   If you must keep parity for legacy tools, mirror ONLY the app.client.engine
#   block below and add a big banner like this one.
# =============================================================================

app:
  client:
    engine:
      mode: localhost
      fixed_host: "10.77.0.10:8000"
      prefer_same_origin: true
      allow_client_override: true

# Everything else lives in app.yaml / race_modes.yaml / event.yaml
# End of config.yaml
